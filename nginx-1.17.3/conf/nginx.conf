
#user  nobody;
worker_processes  1;

#error_log  logs/error.log;
#error_log  logs/error.log  notice;
#error_log  logs/error.log  info;

#pid        logs/nginx.pid;


events {
    worker_connections  1024;
}


http {
    include       mime.types;
    default_type  application/octet-stream;

    #log_format  main  '$remote_addr - $remote_user [$time_local] "$request" '
    #                  '$status $body_bytes_sent "$http_referer" '
    #                  '"$http_user_agent" "$http_x_forwarded_for"';

    #access_log  logs/access.log  main;

    sendfile        on;
    #tcp_nopush     on;

    #keepalive_timeout  0;
    keepalive_timeout  65;

    #gzip  on;

   server {
		listen 443;
        server_name st-ral.cn www.st-ral.cn; 
        ssl on;
        ssl_certificate 1_st-ral.cn_bundle.crt;
        ssl_certificate_key 2_st-ral.cn.key;
        ssl_session_timeout 5m;
        ssl_protocols TLSv1 TLSv1.1 TLSv1.2;
        ssl_ciphers ECDHE-RSA-AES128-GCM-SHA256:HIGH:!aNULL:!MD5:!RC4:!DHE;
        ssl_prefer_server_ciphers on;
		location / {
             proxy_pass http://127.0.0.1:3179;
             root blog;
       }
    }
	server {
		listen 80;
		server_name  st-ral.cn www.st-ral.cn;
		rewrite ^(.*)$ https://${server_name}$1 permanent; 
 	}
}

#tcp{
#	upstream shadowsocks {
#		server 127.0.0.1:38381;
#		}
#	server {
#		listen 38381;
#		proxy_pass shadowsocks;
#	}
#
#}